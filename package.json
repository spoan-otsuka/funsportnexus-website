{
  "name": "funsportnexus-website",
  "type": "module",
  "version": "0.1.0",
  "description": "fun sport nexus 公式サイト (Astro + microCMS + Cloudflare Pages)",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node -e \"require('fs').copyFileSync('public/.assetsignore','dist/.assetsignore')\"",
    "preview": "wrangler pages dev ./dist",
    "deploy": "astro build && wrangler pages deploy ./dist",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^11.2.0",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^4.16.18",
    "microcms-js-sdk": "^3.1.3"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
